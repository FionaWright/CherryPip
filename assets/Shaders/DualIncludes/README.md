# Dual Includes

Files that can be compiled in both C++ and HLSL

Generally used for CBVs, Buffers, etc. Although there are some math functions which are run on both the CPU and GPU, for debugging or otherwise